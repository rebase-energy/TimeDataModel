[project]
name = "timedatamodel"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = ["pint>=0.24", "shapely>=2.0", "numpy>=2.0", "pandas>=2.0"]

[project.optional-dependencies]
polars = ["polars>=1.0"]
dev = ["pytest>=8.0", "polars>=1.0"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
